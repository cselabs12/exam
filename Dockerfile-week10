# Use a lightweight web server like nginx or httpd
FROM nginx:alpine

# Copy the application files to the web server's root directory
COPY . /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Start nginx server
CMD ["nginx", "-g", "daemon off;"]
